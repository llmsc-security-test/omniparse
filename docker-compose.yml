version: "3.9"

services:
  omniparse:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: omniparse-container
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    ports:
      - "11090:8000"
    restart: unless-stopped
